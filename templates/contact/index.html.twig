{% extends 'base.html.twig' %}

{% block body %}
    <h1>Contact</h1>
    {{ form_start(contact) }}
        {{ form_widget(contact) }}

    <button class="btn btn-primary">Envoyer</button>

    {{ form_end(contact) }}
{% endblock %}
